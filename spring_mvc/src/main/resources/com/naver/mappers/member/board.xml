<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
<!-- 매퍼 태그namespace 명을 Board로 지정, mybatis에서 쿼리문을 다루는 태그를 매퍼라고 부른다 -->

<!-- 게시판 저장 -->
<insert id="board_in" parameterType="com.naver.vo.BoardVO">
	<!-- parameterType 속성은 전달인자 타입이다. 일반적으로 생략한다-->
	insert into tbl_board(bno,writer,title,content)
	values(bno_seq.nextval, #{writer},#{title},#{content})
</insert>

</mapper>